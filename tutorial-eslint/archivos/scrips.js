


const number = 2;
	// eslint-disable-next-line no-console
	console.log('number');

const word = 'hola';


window.addEventListener('scroll', () => {
	// eslint-disable-next-line no-console
	console.log('scroll');
});

const suma = number + 2;


const h1 = document.getElementById('h1');

h1.textContent = `${suma} ${word}`;
